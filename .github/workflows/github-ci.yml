name: CI

on: [pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
    
    - name: Flake8
      run: |
        pip install flake8
        flake8 user_service product_service checkout_service